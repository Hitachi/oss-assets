apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: ${{values.name}}
  description: Retrieve ${{values.name}} details
spec:
  type: openapi
  lifecycle: experimental
  owner: backstageadmin
  definition: |
    REPLACE
